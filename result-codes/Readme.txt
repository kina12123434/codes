実行コマンド　(result-mf.cxxのとき)

g++ ../src/vector.cxx ../src/sparseVector.cxx ../src/matrix.cxx result-mf.cxx

./a.out


高精度の判定

→平均の値が高いパラメータの方が精度が良いとして、更新している

【注意】

手法ごとにプログラムを改定し、追加すること。

出力ファイルはあらかじめ作成しておく必要がおそらくある。

-----------------------------------------------------------

以下を変更すれば、使いまわしできる。↓

パラメータを示す、D,beta,alpha  また、パラメータを回しているfor文

MAEdataなどデータを保持するものの欠損数は12で固定しておくこと。

paraMAEなどは、列数がパラメータの個数を示している。

paraMAEにfor文の中でパラメータを代入しているため、変数名や数を変えたときには注意が必要。(78-80行、125-127行)

METHOD_NAME, filenameMAE, filenameAUC

------------------------------------------------------------

出力ファイルの表示形式について

初期値①の出力	初期値②の出力　初期値③の出力　初期値④の出力　初期値⑤の出力　各パラメータ(MF:D beta alpha)

行については、上から欠損数500,1000,1500,...となっている。